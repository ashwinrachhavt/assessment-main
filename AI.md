## AI usage disclosure

I used AI assistance (Codex CLI) to:

- Add GitHub Actions CI for backend + frontend.
- Add/adjust Python tooling (uv in CI, ruff lint/format config, pytest tests).
- Add frontend formatting tooling (Prettier) and format affected files.
